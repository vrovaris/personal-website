---
// Placeholder until P2-01. Exists so the deploy path is proven on day one.
// noindex comes out at launch (Q10).
// Deliberately has no kernel block: the rule must still run unbroken when a
// page has nothing in the narrow track.
import Boundary from '../layouts/Boundary.astro';
---

<Boundary title="vrovaris.com" noindex>
  <h1>vrovaris.com</h1>
  <p>Under construction.</p>
</Boundary>
